public Integer nextPrime(Integer num) {
    num++;
    while(true){
        for(Integer i = 2; i < num; i++){
            if(Math.mod(num,i) == 0){
                num++;
                break;
            }
        }
    return num;
    }
}