public List<Integer> positiveIntegers(List<Integer> numbers) {
    List<Integer> listPosNum = new List<Integer>();
    for(Integer num : numbers){
        if(num > 0)
            listPosNum.add(num);
    }
    return listPosNum;
}