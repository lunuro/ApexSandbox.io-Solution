public List<String> fullNames(List<String> firstNames, List<String> lastNames) {
    List<String> listFullNames = new List<String>();
    for(Integer i = 0; i < firstNames.size(); i++){
        listFullNames.add(firstNames.get(i) + ' ' + lastNames.get(i));
    }
    return listFullNames;
}