public static String reverseWords(String str){
    List<String> listStr = new List<String>();
    for(String st : str.plit(' ')){
        listStr.add(str.reverse());
    }
    return String.join(listStr,' ');
}