public static List<Integer> squareList(List<Integer> nums){
    List<Integer> listSqNums = new List<Integer>();
    for (Integer num : nums){
        listSqNums.add(Math.sqrt(Math.abs(Double.valueOf(num))).intValue());
    }
    listSqNums.sort();
    return listSqNums;
}